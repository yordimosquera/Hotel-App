import styled from 'styled-components';

const PriceCard = styled.View`
  margin-top: 20px;
  flex-direction: row;
  height: 70px;
  align-items: center;
  justify-content: space-around;
  background: rgba(255, 185, 20, 0.85);
`;

export {PriceCard};
