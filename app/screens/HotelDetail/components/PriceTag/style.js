import styled from 'styled-components';

const PriceCard = styled.View`
  margin-top: 20px;
  flex-direction: row;
  height: 70px;
  align-items: center;
  justify-content: space-around;
  background: rgba(255, 40, 2, 0.5);
`;

export {PriceCard};
